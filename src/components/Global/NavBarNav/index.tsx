import styles from ".style.module.scss";
import NavAuth from "../NavAuthDiv";

const NavBar = () => {
    return (
        <nav>
            <img src="" alt="" />
            <NavAuth />
        </nav>
    );
};

export default NavBar;
