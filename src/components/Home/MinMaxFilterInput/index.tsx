import styles from ".style.module.scss";

const MinMaxFilter = () => {
    return (
        <div>
            <input type="number" min="" placeholder="Mínima" />
            <input type="number" max="" placeholder="Máxima" />
        </div>
    );
};

export default MinMaxFilter;
