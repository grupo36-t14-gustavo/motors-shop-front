import styles from ".style.module.scss";
import FilterType from "../FilterTypeLi";

const FilterList = () => {
    return (
        <ul>
            {/* List of FilterType */}
            <FilterType />
        </ul>
    );
};

export default FilterList;
