import styles from ".style.module.scss";

const FilterOptions = () => {
    return (
        <select name="modelo" id="">
            {/* List of options: */}
            <option value="">Porche 718</option>
        </select>
    );
};

export default FilterOptions;
